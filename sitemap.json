# 蛙蛙电子菜谱

这是一个微信原生小程序项目，用于私人菜谱管理。第一阶段只使用微信本地 Storage 保存数据，点单功能仅用于本地记录想做的菜，不包含支付、积分、充值、购物车、库存或商家系统。

## 如何导入微信开发者工具

1. 打开微信开发者工具。
2. 选择“导入项目”。
3. 项目目录选择：`C:\Users\Lenovo\Documents\New project`。
4. AppID 可以先使用测试号或在 `project.config.json` 中把 `appid` 改成你自己的小程序 AppID。
5. 导入后直接编译运行。

## 本地自检

电脑已安装 Node.js 时，可以在项目目录运行：

```bash
node scripts/validate-miniprogram.js
```

看到“蛙蛙电子菜谱自检通过”即可。

## 当前功能

- 菜谱首页：分类栏、菜谱列表、搜索入口、分享入口、右下角添加菜单。
- 点单：从菜谱卡片加入本地点单篮，可加减数量、清空，不会提交到服务器。
- 手动添加和编辑菜谱：支持封面、分类、简介、时间、难度、份量、食材、步骤图片、小贴士、标签、来源链接。
- 菜谱详情：查看、收藏、编辑、删除、分享。
- 分类管理：新增、改名、排序、删除，“全部”分类不可删除。
- 收藏：查看和取消收藏。
- 搜索：按菜名、简介、标签搜索，保存搜索历史。
- 链接导入：目前是模拟解析，不会真实抓取网页，也不会自动保存，必须进入编辑页确认后保存。
- 我的：统计数量、分类管理、数据说明、清除缓存。

## 初始数据

第一次运行时菜谱为空、收藏为空、搜索历史为空。`utils/defaultData.js` 中的 `recipes` 必须保持为空数组，不要添加示例菜谱。

## 图片说明

当前封面图和步骤图使用微信官方 `wx.chooseMedia` 选择本地临时图片，暂时不上传服务器。临时图片路径可能在清理缓存或重新安装后失效。下一阶段可以接入云开发和云存储。

## 下一阶段建议

- 接入云开发，支持多设备同步。
- 增加数据导出和导入。
- 真实链接解析需单独接入合法 API，不建议直接在小程序里做爬虫。
